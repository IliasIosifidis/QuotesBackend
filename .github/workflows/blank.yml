name: Keep alive
on:
  schedule:
    - cron: '*/10 * * * *' # every 10 minutes
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl https://quotesbackend-production-1ee0.up.railway.app/api/user-quotes
